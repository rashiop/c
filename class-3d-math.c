#include <stdio.h>
#include <math.h>

void stringMethod() {
    printf("%f", sqrt(16));
    printf("%f", pow(4, 3));	
    printf("%f", ceil(1.4));
    printf("%f", floor(1.4));
}

int main() {
    stringMethod();
    return 0;
}
